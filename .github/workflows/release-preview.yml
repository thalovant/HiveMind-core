permissions:
  contents: read
  pull-requests: write

name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@e1770fb608f8c39ab886ff336f6470ee17747b0d # dev
    with:
      package_name: 'hivemind_core'
      version_file: 'hivemind_core/version.py'
